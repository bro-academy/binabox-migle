<section class="{{ footerSocialClass }} social">
        <ul class="social__list">
        {% for nav in footerSocial %}
          <li class="social__item">
            <a class="social__link" href="{{ nav.link }}"> {{ nav.name }} </a>
          </li>
        {% endfor %}
        </ul>
</section>