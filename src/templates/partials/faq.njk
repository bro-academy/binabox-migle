<section class="faq">
        <h2 class="faq__title">{{faq.title}}</h2>
        <ul class="faq__list">
          {% for faq in faqs.card %}
          {% include "partials/faq.njk"%}
          {% endfor %}
        </ul>
      </section>
